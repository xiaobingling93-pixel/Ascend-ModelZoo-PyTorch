diff --git a/src/lerobot/configs/eval.py b/src/lerobot/configs/eval.py
index da8bee6b..194f4b0f 100644
--- a/src/lerobot/configs/eval.py
+++ b/src/lerobot/configs/eval.py
@@ -36,6 +36,7 @@ class EvalPipelineConfig:
     output_dir: Path | None = None
     job_name: str | None = None
     seed: int | None = 1000
+    tokenizer_path: str | None = None
     # Rename map for the observation to override the image and state keys
     rename_map: dict[str, str] = field(default_factory=dict)
     # Explicit consent to execute remote code from the Hub (required for hub environments).
diff --git a/src/lerobot/policies/pi05/modeling_pi05.py b/src/lerobot/policies/pi05/modeling_pi05.py
index 104ec63b..c75712fb 100644
--- a/src/lerobot/policies/pi05/modeling_pi05.py
+++ b/src/lerobot/policies/pi05/modeling_pi05.py
@@ -22,6 +22,9 @@ from pathlib import Path
 from typing import TYPE_CHECKING, Literal, TypedDict
 
 import torch
+import torch_npu
+import torchair as tng
+from torchair.configs.compiler_config import CompilerConfig
 import torch.nn.functional as F  # noqa: N812
 from torch import Tensor, nn
 from typing_extensions import Unpack
@@ -51,6 +54,32 @@ from lerobot.utils.constants import (
     OPENPI_ATTENTION_MASK_VALUE,
 )
 
+import transformers
+
+
+def apply_npu_rope(q, k, cos, sin, position_ids=None, unsqueeze_dim=1):
+    cos = cos.unsqueeze(unsqueeze_dim)
+    sin = sin.unsqueeze(unsqueeze_dim)
+    n_q = q.shape[1]
+    n_k = k.shape[1]
+    merged_states = torch.cat([q, k], dim=1) # [B, n_q + n_k, S, D]
+    merged_rot = torch_npu.npu_rotary_mul(merged_states, cos, sin)
+    q_embed, k_embed = merged_rot.split([n_q, n_k], dim=1)
+    return q_embed, k_embed
+
+
+transformers.models.gemma.modeling_gemma.apply_rotary_pos_emb = apply_npu_rope
+
+
+def format_cast_to_NZ(model):
+    FRACTAL_NZ = 29
+    for name, module in model.named_modules():
+        if isinstance(module, nn.Linear):
+            if hasattr(module, 'weight') and module.weight is not None:
+                weight_nz = torch_npu.npu_format_cast(module.weight, FRACTAL_NZ)
+                module.weight = nn.Parameter(weight_nz, requires_grad=False)
+                print(f"✅  converted:  {name} -> FRACTAL_NZ")
+    return model
 
 class ActionSelectKwargs(TypedDict, total=False):
     inference_delay: int | None
@@ -188,7 +217,7 @@ def resize_with_pad_torch(  # see openpi `resize_with_pad_torch` (exact copy)
     # Handle dtype-specific clipping
     if images.dtype == torch.uint8:
         resized_images = torch.round(resized_images).clamp(0, 255).to(torch.uint8)
-    elif images.dtype == torch.float32:
+    elif images.dtype == torch.float32 or images.dtype == torch.float16:
         resized_images = resized_images.clamp(-1.0, 1.0)
     else:
         raise ValueError(f"Unsupported image dtype: {images.dtype}")
@@ -335,7 +364,7 @@ class PaliGemmaWithExpertModel(
         vlm_config,
         action_expert_config,
         use_adarms=None,
-        precision: Literal["bfloat16", "float32"] = "bfloat16",
+        precision: Literal["float16", "float32"] = "float16",
         image_size: int = DEFAULT_IMAGE_SIZE,
         freeze_vision_encoder: bool = False,
         train_expert_only: bool = False,
@@ -365,6 +394,7 @@ class PaliGemmaWithExpertModel(
         vlm_config_hf.vision_config.projection_dim = 2048
         vlm_config_hf.vision_config.projector_hidden_act = "gelu_fast"
         vlm_config_hf.vision_config.torch_dtype = "float32"
+        vlm_config_hf.vision_config._attn_implementation = "eager"
 
         action_expert_config_hf = CONFIG_MAPPING["gemma"](
             head_dim=action_expert_config.head_dim,
@@ -384,12 +414,12 @@ class PaliGemmaWithExpertModel(
         self.gemma_expert = GemmaForCausalLM(config=action_expert_config_hf)
         self.gemma_expert.model.embed_tokens = None
 
-        self.to_bfloat16_for_selected_params(precision)
+        self.to_float16_for_selected_params(precision)
         self._set_requires_grad()
 
-    def to_bfloat16_for_selected_params(self, precision: Literal["bfloat16", "float32"] = "bfloat16"):
-        if precision == "bfloat16":
-            self.to(dtype=torch.bfloat16)
+    def to_float16_for_selected_params(self, precision: Literal["float16", "float32"] = "float16"):
+        if precision == "float16":
+            self.to(dtype=torch.float16)
         elif precision == "float32":
             self.to(dtype=torch.float32)
             return
@@ -551,7 +581,7 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
             paligemma_config,
             action_expert_config,
             use_adarms=[False, True],
-            precision=config.dtype,
+            precision="float16",
             image_size=config.image_resolution[0],
             freeze_vision_encoder=config.freeze_vision_encoder,
             train_expert_only=config.train_expert_only,
@@ -569,9 +599,13 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         # Compile model if requested
         if config.compile_model:
             torch.set_float32_matmul_precision("high")
-            self.sample_actions = torch.compile(self.sample_actions, mode=config.compile_mode)
-            # Also compile the main forward pass used during training
-            self.forward = torch.compile(self.forward, mode=config.compile_mode)
+            config = CompilerConfig()
+            config.experimental_config.frozen_parameter = True
+            config.experimental_config.tiling_schedule_optimize = True  # 使能tiling全下沉配置
+            npu_backend = tng.get_npu_backend(compiler_config=config)
+            self.paligemma_with_expert.forward = torch.compile(self.paligemma_with_expert.forward, dynamic=False, fullgraph=True, backend=npu_backend)
+            self.paligemma_with_expert.embed_image = torch.compile(self.paligemma_with_expert.embed_image, dynamic=False, fullgraph=True, backend=npu_backend)
+            self.denoise_step = torch.compile(self.denoise_step, dynamic=False, fullgraph=True, backend=npu_backend)
 
         msg = """An incorrect transformer version is used, please create an issue on https://github.com/huggingface/lerobot/issues"""
 
@@ -620,8 +654,8 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
             mean=0.0,
             std=1.0,
             size=shape,
-            dtype=torch.float32,
-            device=device,
+            dtype=torch.float16,
+            device=device
         )
 
     def sample_time(self, bsize, device):
@@ -640,17 +674,20 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         att_masks = []
 
         # Process images
-        for img, img_mask in zip(images, img_masks, strict=True):
+        batch_size = images[0].shape[0] # correspond to 1 img, therefore batch_size is 1
+        image_batch = torch.cat(images, dim=0)
+        def image_embed_func(img):
+            return self.paligemma_with_expert.embed_image(img)
 
-            def image_embed_func(img):
-                return self.paligemma_with_expert.embed_image(img)
+        img_emb = self._apply_checkpoint(image_embed_func, image_batch)
+        num_img_embs_per_img = img_emb.shape[1]
+        img_emb = img_emb.view(batch_size, -1, img_emb.shape[-1]) # keep batch size to 1, stack on the second dim
+        bsize, num_img_embs = img_emb.shape[:2]
+        embs.append(img_emb)
 
-            img_emb = self._apply_checkpoint(image_embed_func, img)
-            bsize, num_img_embs = img_emb.shape[:2]
-
-            embs.append(img_emb)
-            pad_masks.append(img_mask[:, None].expand(bsize, num_img_embs))
-            att_masks += [0] * num_img_embs
+        att_masks += [0] * num_img_embs
+        img_mask = torch.stack(img_masks).expand(-1, num_img_embs_per_img).reshape(batch_size, -1) # same, keep batch size to 1
+        pad_masks.append(img_mask)
 
         # Process language tokens
         def lang_embed_func(tokens):
@@ -688,7 +725,7 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
             max_period=self.config.max_period,
             device=timestep.device,
         )
-        time_emb = time_emb.type(dtype=timestep.dtype)
+        time_emb = time_emb.to(dtype=timestep.dtype)
 
         # Fuse timestep + action information using an MLP
         def action_proj_func(noisy_actions):
@@ -823,7 +860,7 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         x_t = noise
         for step in range(num_steps):
             time = 1.0 + step * dt
-            time_tensor = torch.tensor(time, dtype=torch.float32, device=device).expand(bsize)
+            time_tensor = torch.tensor(time, dtype=torch.float16, device=device).expand(bsize)
 
             def denoise_step_partial_call(input_x_t, current_timestep=time_tensor):
                 return self.denoise_step(
@@ -891,7 +928,6 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
 
         suffix_out = outputs_embeds[1]
         suffix_out = suffix_out[:, -self.config.chunk_size :]
-        suffix_out = suffix_out.to(dtype=torch.float32)
         return self.action_out_proj(suffix_out)
 
 
@@ -916,13 +952,13 @@ class PI05Policy(PreTrainedPolicy):
 
         # Initialize the core PI05 model
         self.init_rtc_processor()
-        self.model = PI05Pytorch(config, rtc_processor=self.rtc_processor)
+        self.model = PI05Pytorch(config, rtc_processor=self.rtc_processor).to(torch.float16)
 
         # Enable gradient checkpointing if requested
         if config.gradient_checkpointing:
             self.model.gradient_checkpointing_enable()
 
-        self.model.to(config.device)
+        self.model.to(device="npu")
 
         self.reset()
 
@@ -1046,6 +1082,7 @@ class PI05Policy(PreTrainedPolicy):
         except Exception as e:
             print(f"Warning: Could not remap state dict keys: {e}")
 
+        model = format_cast_to_NZ(model)
         return model
 
     def _fix_pytorch_state_dict_keys(
@@ -1158,15 +1195,8 @@ class PI05Policy(PreTrainedPolicy):
                 img = img.to(device)
 
             # Ensure float32 dtype for consistency
-            if img.dtype != torch.float32:
-                img = img.to(torch.float32)
-
-            # from openpi preprocess_observation_pytorch: Handle both [B, C, H, W] and [B, H, W, C] formats
-            is_channels_first = img.shape[1] == 3  # Check if channels are in dimension 1
-
-            if is_channels_first:
-                # Convert [B, C, H, W] to [B, H, W, C] for processing
-                img = img.permute(0, 2, 3, 1)
+            if img.dtype != torch.float16:
+                img = img.to(torch.float16)
 
             # from openpi preprocess_observation_pytorch: Resize with padding if needed
             if img.shape[1:3] != self.config.image_resolution:
@@ -1175,10 +1205,6 @@ class PI05Policy(PreTrainedPolicy):
             # Normalize from [0,1] to [-1,1] as expected by siglip
             img = img * 2.0 - 1.0
 
-            # from openpi preprocess_observation_pytorch: Convert back to [B, C, H, W] format if it was originally channels-first
-            if is_channels_first:
-                img = img.permute(0, 3, 1, 2)  # [B, H, W, C] -> [B, C, H, W]
-
             images.append(img)
             # Create mask (all ones for real images)
             bsize = img.shape[0]
@@ -1206,8 +1232,6 @@ class PI05Policy(PreTrainedPolicy):
             "RTC is not supported for select_action, use it with predict_action_chunk"
         )
 
-        self.eval()
-
         # Action queue logic for n_action_steps > 1
         if len(self._action_queue) == 0:
             actions = self.predict_action_chunk(batch)[:, : self.config.n_action_steps]
@@ -1219,7 +1243,6 @@ class PI05Policy(PreTrainedPolicy):
     @torch.no_grad()
     def predict_action_chunk(self, batch: dict[str, Tensor], **kwargs: Unpack[ActionSelectKwargs]) -> Tensor:
         """Predict a chunk of actions given environment observations."""
-        self.eval()
 
         # Prepare inputs
         images, img_masks = self._preprocess_images(batch)
diff --git a/src/lerobot/scripts/lerobot_eval.py b/src/lerobot/scripts/lerobot_eval.py
index e32b8040..20994823 100644
--- a/src/lerobot/scripts/lerobot_eval.py
+++ b/src/lerobot/scripts/lerobot_eval.py
@@ -61,10 +61,16 @@ from pathlib import Path
 from pprint import pformat
 from typing import Any, TypedDict
 
+import torch
+import torch_npu
+from torch_npu.contrib import transfer_to_npu
+torch.npu.set_device(
+    "npu:0"
+) # 必须在import gymnasium之前设置，否则仿真环境可能跟NPU有冲突
 import einops
 import gymnasium as gym
 import numpy as np
-import torch
+
 from termcolor import colored
 from torch import Tensor, nn
 from tqdm import trange
@@ -538,6 +544,8 @@ def eval_main(cfg: EvalPipelineConfig):
         "device_processor": {"device": str(policy.config.device)},
         "rename_observations_processor": {"rename_map": cfg.rename_map},
     }
+    if cfg.tokenizer_path:
+        preprocessor_overrides['tokenizer_processor'] = {"tokenizer_name": cfg.tokenizer_path}
 
     preprocessor, postprocessor = make_pre_post_processors(
         policy_cfg=cfg.policy,
@@ -804,6 +812,7 @@ def eval_policy_all(
 
 
 def main():
+    torch_npu.npu.set_compile_mode(jit_compile=False)
     init_logging()
     register_third_party_plugins()
     eval_main()
diff --git a/src/lerobot/utils/utils.py b/src/lerobot/utils/utils.py
index c7ad2bbd..c46595fb 100644
--- a/src/lerobot/utils/utils.py
+++ b/src/lerobot/utils/utils.py
@@ -114,6 +114,8 @@ def is_torch_device_available(try_device: str) -> bool:
         return torch.xpu.is_available()
     elif try_device == "cpu":
         return True
+    elif try_device == "npu":
+        return torch.npu.is_available()
     else:
         raise ValueError(f"Unknown device {try_device}. Supported devices are: cuda, mps, xpu or cpu.")
 
